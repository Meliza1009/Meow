{
  "name": "physical-zip",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PHYSICAL ZIP — compress a human into a .zip, then extract them.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "0.10.35"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.12",
    "tailwindcss": "^4.1.12",
    "typescript": "~5.9.2",
    "vite": "^7.1.7"
  }
}
